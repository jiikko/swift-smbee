name: E2E

on:
  workflow_dispatch:
  push:
    branches: [master]
  pull_request:

permissions:
  contents: read

concurrency:
  group: e2e-${{ github.ref }}
  cancel-in-progress: true

jobs:
  e2e-api:
    name: API E2E
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      SAMBA_CONTAINER: smbee-samba-api
      SAMBA_BASE_IMAGE: ubuntu:24.04
      SAMBA_CONFIG: test/e2e/smb/smb302-encrypted-required.conf
      SMBEE_E2E_PROFILE: smb302-encrypted-required
      SMBEE_E2E_HOST: 127.0.0.1
      SMBEE_E2E_PORT: "445"
    steps:
      - uses: actions/checkout@v4

      - name: Set up Swift
        uses: swift-actions/setup-swift@v2
        with:
          swift-version: "6.0"
      - run: swift --version

      - name: Start Samba
        run: bash test/e2e/start-samba-ci.sh

      - name: Probe negotiated parameters
        env:
          SMBEE_DEBUG: "1"
        run: swift run smbcli probe "smb://${SMBEE_E2E_HOST}:${SMBEE_E2E_PORT}"

      - name: Samba-backed API E2E tests
        env:
          SMBEE_E2E: "1"
          SMBEE_DEBUG: "1"
        run: |
          set -euxo pipefail
          swift test --filter SMBeeE2ETests

      - name: Samba logs (debug)
        if: always()
        run: docker logs "${SAMBA_CONTAINER}" || true

      - name: Teardown
        if: always()
        run: docker rm -f "${SAMBA_CONTAINER}" || true

  e2e-cli:
    name: smbcli E2E
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      SAMBA_CONTAINER: smbee-samba-cli
      SAMBA_BASE_IMAGE: ubuntu:24.04
      SAMBA_CONFIG: test/e2e/smb/smb302-encrypted-required.conf
      SMBEE_E2E_PROFILE: smb302-encrypted-required
      SMBEE_E2E_HOST: 127.0.0.1
      SMBEE_E2E_PORT: "445"
    steps:
      - uses: actions/checkout@v4

      - name: Set up Swift
        uses: swift-actions/setup-swift@v2
        with:
          swift-version: "6.0"
      - run: swift --version

      - name: Start Samba
        run: bash test/e2e/start-samba-ci.sh

      - name: Build smbcli
        run: swift build --product smbcli

      - name: smbcli command smoke
        env:
          SMB_PASSWORD: smbee
        run: |
          set -euxo pipefail
          SMBCLI=".build/debug/smbcli"
          SMB_URL="smb://smbee@${SMBEE_E2E_HOST}:${SMBEE_E2E_PORT}/public"
          root="smbee-gha-cli-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          local_dir="$(mktemp -d)"
          cleanup() {
            "${SMBCLI}" rm -r "${SMB_URL}/${root}" || true
            rm -rf "${local_dir}"
          }
          trap cleanup EXIT

          mkdir -p "${local_dir}/dir/child"
          printf "hello smbee command smoke\n" > "${local_dir}/file.txt"
          printf "nested payload\n" > "${local_dir}/dir/child/nested.txt"

          "${SMBCLI}" ls "${SMB_URL}"
          "${SMBCLI}" mkdir "${SMB_URL}/${root}"
          "${SMBCLI}" put "${local_dir}/file.txt" "${SMB_URL}/${root}/file.txt"
          "${SMBCLI}" cat "${SMB_URL}/${root}/file.txt" | cmp "${local_dir}/file.txt" -
          "${SMBCLI}" get "${SMB_URL}/${root}/file.txt" "${local_dir}/downloaded.txt"
          cmp "${local_dir}/file.txt" "${local_dir}/downloaded.txt"
          "${SMBCLI}" cp "${SMB_URL}/${root}/file.txt" "${SMB_URL}/${root}/copied.txt"
          "${SMBCLI}" cat "${SMB_URL}/${root}/copied.txt" | cmp "${local_dir}/file.txt" -
          "${SMBCLI}" put -r "${local_dir}/dir" "${SMB_URL}/${root}/uploaded-dir"
          "${SMBCLI}" ls "${SMB_URL}/${root}/uploaded-dir/child"
          "${SMBCLI}" get -r "${SMB_URL}/${root}/uploaded-dir" "${local_dir}/downloaded-dir"
          cmp "${local_dir}/dir/child/nested.txt" "${local_dir}/downloaded-dir/child/nested.txt"
          "${SMBCLI}" cp -r "${SMB_URL}/${root}/uploaded-dir" "${SMB_URL}/${root}/copied-dir"
          "${SMBCLI}" cat "${SMB_URL}/${root}/copied-dir/child/nested.txt" | cmp "${local_dir}/dir/child/nested.txt" -
          "${SMBCLI}" rm -r "${SMB_URL}/${root}"

      - name: Samba logs (debug)
        if: always()
        run: docker logs "${SAMBA_CONTAINER}" || true

      - name: Teardown
        if: always()
        run: docker rm -f "${SAMBA_CONTAINER}" || true
